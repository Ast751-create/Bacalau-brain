# 🐟 Bacalhau Brain — Тренажёр португальского

Веб-приложение для изучения португальской лексики с мнемоническими ассоциациями.

## 🚀 Быстрый старт

1. Открой `index.html` в браузере — или опубликуй на GitHub Pages
2. Выбери список слов в сайдбаре
3. Нажми "Начать" и учи слова!

## 📁 Структура проекта

```
├── index.html          # Главная страница
├── styles.css          # Стили
├── app.js              # Логика приложения
├── vocabulary.js       # Словарь (слова + мнемоники)
├── fetch-images.js     # Скрипт загрузки картинок с Unsplash
├── apply-images.js     # Скрипт применения картинок к словарю
├── image-urls.json     # Кэш URL картинок
└── .github/workflows/  # Автоматизация GitHub Actions
```

## 🔑 API ключи

### OpenAI (для генерации мнемоник)
1. Получи ключ: https://platform.openai.com/api-keys
2. В приложении: ⚙️ Настройки → вставь ключ
3. Или сохрани в `.env.local` (не коммитить!):
   ```
   OPENAI_API_KEY=sk-proj-xxx...
   ```

### Unsplash (для картинок)
Ключ уже встроен в `fetch-images.js`. Лимит: 50 запросов/час.

## 🖼️ Загрузка картинок

### Вручную
```bash
node fetch-images.js   # Загрузить URL картинок (до 50 шт)
node apply-images.js   # Применить к vocabulary.js
git add -A && git commit -m "Add images" && git push
```

### Автоматически (GitHub Actions)
Workflow `.github/workflows/fetch-images.yml` запускается каждые 2 часа и:
1. Загружает до 50 картинок
2. Применяет к словарю
3. Коммитит изменения

**Ручной запуск:** GitHub → Actions → "Fetch Unsplash Images" → Run workflow

## 📚 Добавление слов

### В vocabulary.js (VOCABULARY)
```javascript
{ 
    pt: "palavra",           // Португальское слово
    ru: "слово",             // Перевод
    imageQuery: "word text", // Запрос для картинки
    soundHint: "ПАЛка для СЛОВА" // Мнемоника (метод ключевого слова)
}
```

### В тематические списки (THEMED_LISTS)
```javascript
"tema_a2": {
    name: "Básico",
    category: "tema",
    categoryName: "📚 Тема",
    words: [...]
}
```

## 🧠 Правила мнемоник

**Метод ключевого слова:**
1. Найди русское слово, которое ЗВУЧИТ похоже на португальское
2. Создай АБСУРДНУЮ сцену, где это слово взаимодействует со значением
3. Добавь ДЕЙСТВИЕ, ЭМОЦИЮ или ШОКОВЫЙ элемент

**Примеры:**
- `furacão` (ураган) → "ФУРА попала в ураган и летит по небу как птица"
- `abelha` (пчела) → "АБЕЛЬ орёт — его ПЧЕЛА ужалила прямо в нос!"

## 🔄 Функция повторения

- Неправильный ответ → слово автоматически в список повторения
- 2 правильных ответа подряд → слово удаляется из списка
- Панель снизу показывает слова для повторения

## 🌐 Деплой на GitHub Pages

1. GitHub → Settings → Pages
2. Source: Deploy from branch → main → / (root)
3. Save
4. Сайт будет на: `https://username.github.io/repo-name/`

## 🛠️ Технологии

- Vanilla JS (без фреймворков)
- localStorage для сохранения прогресса
- Web Speech API для произношения
- Unsplash API для картинок
- OpenAI API для генерации мнемоник

## 📝 Лицензия

MIT — делай что хочешь!
